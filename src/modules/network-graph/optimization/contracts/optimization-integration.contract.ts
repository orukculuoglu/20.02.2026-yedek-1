/**
 * REMOVED: Integration Framework Scope Correction
 * 
 * This file previously contained integration boundary contracts.
 * These have been removed to keep the optimization layer as a pure boundary
 * definition layer, not an integration framework.
 * 
 * Integration boundaries are documented in:
 * - src/modules/network-graph/optimization/index.ts (INTEGRATION BOUNDARIES section)
 * 
 * Existing contracts provide all necessary clarity:
 * - OptimizationCandidateAction, OptimizationInput, OptimizationResult, etc.
 */
