/**
 * Examples / Verification Layer
 * Deterministic example artifacts and verification scenarios.
 * No business logic, scoring, or interpretation.
 * 
 * Purpose:
 * Exercise the temporal foundation (contracts, entities, builders, operators)
 * without introducing runtime orchestration or semantic interpretation.
 * 
 * Scope:
 * - Example window identities (ExampleIdentities)
 * - Example entities built via builders (ExampleEntities)
 * - Example collection operations (ExampleCollections)
 * - Example partitioning scenarios (ExamplePartitions)
 */

export * from "./ExampleIdentities.ts";
export * from "./ExampleEntities.ts";
export * from "./ExampleCollections.ts";
export * from "./ExamplePartitions.ts";
